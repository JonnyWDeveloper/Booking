@model Booking.Core.Entities.GymClass

@{
    ViewData["Title"] = "Details";
}

<br />
<h1>Gym Class</h1>
<h3>Booked single session</h3>
<br />

<table class="table">
    <thead>
        <tr>
            <th>Bookable&nbsp;&nbsp;&nbsp;<span style="font-size:.5rem;" class="bg-success bg-opacity-25">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span></th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>

    <tbody id="gymclasses">
        <partial name="GymClassPartialDetails" />

    </tbody>
</table>


<div class="row">
    <br />
    <br />
</div>
<div class="row">
    <h5>Booked by members:</h5>
    <br />
    <br />

    @foreach (var member in Model.AttendingMembers)
    {
        if (member.ApplicationUserId != null)
        {
            <p>@member.ApplicationUser.Email</p>
            @*<p>ApplicationUserId: @member.ApplicationUserId</p>*@
            @*<p>ApplicationUser: @member.ApplicationUser</p>*@
            @*  <p>GymClassId: @member.GymClassId</p>
    <p>GymClass.Name: @member.GymClass.Name</p>
    <p>No. of AttendingMembers: @member.GymClass.AttendingMembers.Count</p>*@
        }
    }
</div>

<div>
    @*<a asp-action="Edit" asp-route-id="@Model.GetEnumerator().Current.Id">Edit</a> *@
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>